namespace FileIntegrityChecker.Enums;

/// <summary>
/// Defines the types of scans that can be performed.
/// </summary>
// OOP: Enum (Provides discrete set of values, improving type safety)
public enum ScanType
{
    Quick,
    Deep,
    Baseline
}
